import { Routes } from '@angular/router';
import { LogReaderComponent } from './pages/log-reader/log-reader.component';

export const routes: Routes = [
    {
        path: '',
        component: LogReaderComponent,
    },
];
